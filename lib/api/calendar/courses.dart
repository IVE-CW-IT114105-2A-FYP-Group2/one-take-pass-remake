import 'package:one_take_pass_remake/api/calendar/calendar.dart';

class Courses implements CalendarInterface {
  @override
  DateTime get end => throw UnimplementedError();

  @override
  DateTime get start => throw UnimplementedError();
}
